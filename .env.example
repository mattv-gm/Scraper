# Facebook credentials (optional — only needed for login-gated content)
FB_EMAIL=your_email@example.com
FB_PASSWORD=your_password

# Instagram credentials (optional — only needed for login-gated content)
IG_USERNAME=your_username
IG_PASSWORD=your_password
